from setuptools import setup, find_packages

setup(
    name="repday",
    version="0.1",
    description="Representative-day duration-curve error minimization with Pyomo",
    packages=find_packages(),
)
